<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        * {
            padding: 0;
            margin: 0;
        }
        html, body {
            width: 100%;
            height: 100%;
            overflow: hidden;
        }
        .container {
            width: 50%;
            min-width: 500px;
            border: 1px solid #eee;
            border-radius: 5px;
            margin: auto;
            padding: 10px;
        }
        ul.list, li.item {
            list-style: none;
        }

        li.item {
            
            padding: 5px;
            border-bottom: 1px solid #eee;
        }

        li.item:last-child {
            border-bottom: none;
        }
        
        .file-link {
            color: blue;
            text-decoration: underline;
            cursor: pointer;
        }
    </style>
</head>
<body>
    文件目录：
    <div class="container">
        <ul class="list">
            <% for (let i = 0; i < dirents.length; i++) { %>
                <% const dirent = dirents[i]; %>
                <li class="item">
                    <% if (dirent.isFile()) { %>
                        <a class="file-link" href="<%= dirent.path %>" download><%= dirents[i].name %></a>
                    <% } else if (dirent.isDirectory()) { %>
                        <a class="dir-link" href="<%= dirent.path %>"><%= dirent.name %></a>
                    <% } else {%>
                        <span><%= dirent.name %></span>
                    <% } %>
                </li>
            <% } %>
        </ul>
    </div>
    <div class="footer">

    </div>
</body>
</html>